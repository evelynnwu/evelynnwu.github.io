import React from 'react';
interface Props {
    background: string;
    borderWidth: string;
    characterWidth: string | undefined;
    prevValue: string;
    step: number;
    textColor: string;
    value: string;
    withSound?: boolean | string;
}
export default function CharacterComponent({ background, borderWidth, characterWidth, prevValue, step, textColor, value, withSound, }: Props): React.JSX.Element;
export {};

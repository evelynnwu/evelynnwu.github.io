import React from 'react';
interface PanelProps {
    background: string;
    position: string;
    textColor: string;
    value: string;
}
export default function Panel(props: PanelProps): React.JSX.Element;
interface FlipPanelProps extends PanelProps {
    direction: string;
    duration: string;
}
export declare function FlipPanel({ background, direction, duration, position, textColor, value }: FlipPanelProps): React.JSX.Element;
export {};

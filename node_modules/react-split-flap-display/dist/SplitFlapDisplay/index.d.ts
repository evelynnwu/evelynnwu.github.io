import React from 'react';
export interface SplitFlapDisplayProps extends React.HTMLProps<HTMLDivElement> {
    background?: string;
    borderColor?: string;
    borderWidth?: string;
    characterSet: string[];
    characterWidth?: string;
    fontSize?: string;
    minLength?: number;
    padDirection?: string;
    step?: number;
    textColor?: string;
    value: string;
    withSound?: boolean | string;
}
export default function SplitFlapDisplay({ className, background, borderColor, borderWidth, characterSet, characterWidth, fontSize, minLength, padDirection, step, textColor, value, withSound, ...rest }: SplitFlapDisplayProps): React.JSX.Element;

import React from 'react';
type CodeBlockProps = {
    background: string;
    borderColor: string;
    borderWidth: string;
    characterSet: string;
    characterWidth: string;
    fontSize: string;
    minLength: number;
    padDirection: string;
    step: number;
    textColor: string;
    value: string;
    withSound: string;
};
export default function CodeBlock({ background, borderColor, borderWidth, characterSet, characterWidth, fontSize, minLength, padDirection, step, textColor, value, withSound, }: CodeBlockProps): React.JSX.Element;
export {};

import React from 'react';
import { ColorChangeHandler } from 'react-color';
interface Props {
    color: string;
    onChange: ColorChangeHandler;
}
export default function Swatch({ color, onChange }: Props): React.JSX.Element;
export {};
